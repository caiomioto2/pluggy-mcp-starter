# Security policy

Do not open the HTTP endpoint to the public internet without HTTPS and a strong `MCP_HTTP_TOKEN`.

Never commit `.env`, Pluggy credentials, Item IDs, account identifiers, or financial data. Report vulnerabilities privately through the repository security advisory feature.
